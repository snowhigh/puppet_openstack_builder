#!/bin/bash

# This is a stub script that does almost nothing other
# than notifying the user that defaults are being used.
# It's purpose is to allow explcit shell overrides of
# of vendor packaging.

echo "Using non-vendor defaults."
